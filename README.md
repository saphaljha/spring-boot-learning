# Spring Boot

## Learning Path


Core Java (OOP, Collections, Exceptions)
   ↓
Modern Java (Streams, Lambdas, Records)
   ↓
Build Tools (Maven) & Basic SQL / JDBC
   ↓
Spring Framework Basics (IoC, DI, Beans)
   ↓
Spring Boot Web (REST APIs, Validation, Global Exceptions)
   ↓
Spring Data JPA (Hibernate, Databases, Migrations)
   ↓
Spring Security (JWT, RBAC)
   ↓
Testing (JUnit, Mockito) & Actuator / Docker
